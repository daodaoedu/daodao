# Codex／ChatGPT 桌面版（.agents/skills）

<!-- GENERATED by plugin/scripts/build.ts — 不要直接改這個檔案，改 plugin/skills/<name>/ 後重跑 pnpm plugin:build -->

canonical 在 `plugin/skills/`。這裡是完整複本，不是薄入口——`.agents/skills/` 是 Codex 與 ChatGPT 桌面版
實際掃描的目錄（`$CWD`、`$CWD/..`、`$REPO_ROOT` 三層都會掃），內容必須自足。

| Skill | 收錄 | 備註 |
|---|---|---|
| code-review | ✅ | 完整 |
| collect-pr-feedback | ✅ | 完整 |
| dev-task | ✅ | 降級：browser、drive |
| file-bug-issue | ✅ | 完整 |
| format-commit | ✅ | 完整 |
| gh-card | ✅ | 完整 |
| gh-pipeline | ✅ | 完整 |
| notify-related-issue | ✅ | 完整 |
| post-merge-wrapup | ✅ | 完整 |
| prd-generation | ✅ | 完整 |
| pre-commit-check | ✅ | 完整 |
| product-status-check | ✅ | 完整 |
| publish-tasks | ✅ | 完整 |

## 閘門

Codex 有自己的 hooks（事件名與 Claude Code 相同）。本 repo 的 .codex/hooks.json 已指向同一批腳本，所以 pre-pr-gate 等閘門在 Codex 也會實際觸發。仍要確認 /hooks 已信任該設定，未信任時視同沒有閘門。

## 其餘落差

- **browser**：這裡預設沒有瀏覽器工具。需要畫面驗證時標記為未完成，或請人工執行後貼回證據；不得只因程式碼看起來對就打勾。
- **drive**：沒有 Drive connector。驗證報告先存成本機 Markdown，上傳由人工或 Claude Code 完成。
